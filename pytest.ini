[pytest]
# Scope the offline smoke suite to tests/ only. The legacy backend/test_*.py
# scripts are manual integration probes (they make real calls) and must not be
# auto-collected by `pytest -q`.
testpaths = tests
addopts = -p no:cacheprovider
